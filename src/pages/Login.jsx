import { useState } from 'react'; import { useQueryClient } from '@tanstack/react-query'; import { login, register } from '../api/auth'
export function Login(){const client=useQueryClient();const [mode,setMode]=useState('login');const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function submit(e){e.preventDefault();const f=new FormData(e.currentTarget);setBusy(true);setError('');try{await (mode==='login'?login({email:f.get('email'),password:f.get('password')}):register({email:f.get('email'),password:f.get('password'),base_currency:f.get('currency')}));await client.invalidateQueries({queryKey:['me']})}catch(e){setError(e.message)}finally{setBusy(false)}}return <div className="auth"><section className="auth-intro"><div className="brand"><span className="brand-mark">F</span><span>fluent</span></div><h1>See where every peso goes.</h1><p>A private, calm home for your money. Track spending, move money between accounts, and stay ahead of every card payment.</p></section><section className="auth-form"><form className="card" onSubmit={submit}><h2>{mode==='login'?'Welcome back':'Start your ledger'}</h2><p className="section-description">{mode==='login'?'Sign in to your personal finance tracker.':'Create your private personal finance tracker.'}</p><div className="field"><label>Email</label><input type="email" name="email" required autoFocus/></div><div className="field" style={{marginTop:14}}><label>Password</label><input type="password" name="password" minLength="8" required/></div>{mode==='register'&&<div className="field" style={{marginTop:14}}><label>Base currency</label><select name="currency" defaultValue="MXN"><option>MXN</option><option>USD</option><option>EUR</option></select></div>}{error&&<p className="error" style={{marginTop:14}}>{error}</p>}<button className="button" style={{width:'100%',justifyContent:'center',marginTop:20}} disabled={busy}>{busy?'Working…':mode==='login'?'Sign in':'Create account'}</button><button type="button" className="button secondary" style={{width:'100%',justifyContent:'center',marginTop:10}} onClick={()=>{setMode(mode==='login'?'register':'login');setError('')}}>{mode==='login'?'Need an account? Register':'Already have an account? Sign in'}</button></form></section></div>}
