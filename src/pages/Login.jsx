import { useState } from 'react'; import { useQueryClient } from '@tanstack/react-query'; import { login } from '../api/auth'
export function Login(){const client=useQueryClient();const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function submit(e){e.preventDefault();const f=new FormData(e.currentTarget);setBusy(true);setError('');try{await login({email:f.get('email'),password:f.get('password')});await client.invalidateQueries({queryKey:['me']})}catch(e){setError(e.message)}finally{setBusy(false)}}return <div className="auth"><section className="auth-intro"><div className="brand"><span className="brand-mark">F</span><span>fluent</span></div><h1>See where every peso goes.</h1><p>A private, calm home for your money. Track spending, move money between accounts, and stay ahead of every card payment.</p></section><section className="auth-form"><form className="card" onSubmit={submit}><h2>Welcome back</h2><p className="section-description">Sign in with the account provided by your administrator.</p><div className="field"><label>Email</label><input type="email" name="email" required autoFocus/></div><div className="field" style={{marginTop:14}}><label>Password</label><input type="password" name="password" minLength="8" required/></div>{error&&<p className="error" style={{marginTop:14}}>{error}</p>}<button className="button" style={{width:'100%',justifyContent:'center',marginTop:20}} disabled={busy}>{busy?'Working…':'Sign in'}</button></form></section></div>}
