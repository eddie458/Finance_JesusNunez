export function formatCents(value, currency = 'MXN') { const code=String(currency||'MXN').toUpperCase();const amount=(Number(value)||0)/100;const symbol={MXN:'$',USD:'$',EUR:'€'}[code]||'';const number=new Intl.NumberFormat('en-US',{minimumFractionDigits:2,maximumFractionDigits:2}).format(Math.abs(amount));return `${amount<0?'-':''}${symbol}${number} ${code}`.trim() }
export function parseToCents(value) { const normalized=String(value ?? '').trim().replace(/[$,\s]/g,''); if (!/^-?\d+(\.\d{0,2})?$/.test(normalized)) return null; const negative=normalized.startsWith('-'); const [whole, decimal='']=normalized.replace('-','').split('.'); const cents=Number(whole)*100+Number(decimal.padEnd(2,'0')); return negative?-cents:cents }
